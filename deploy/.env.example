# Copy to .env on the Pi (same folder as docker-compose.yml).

DJ_ENV=production
DJ_LOG_LEVEL=INFO

# https://acoustid.org/new-application
DJ_ACOUSTID_API_KEY=

# Only change if your Docker Hub username is not hannibalov
# DJ_BACKEND_IMAGE=youruser/dj-pipeline-backend:latest
# DJ_FRONTEND_IMAGE=youruser/dj-pipeline-frontend:latest
