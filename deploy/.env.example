# Copy to .env on the Pi (same folder as docker-compose.yml).

# Your Docker Hub ID — same as DOCKER_USER when you ran publish-dockerhub.sh on the Mac
DOCKER_USER=youruser

DJ_ENV=production
DJ_LOG_LEVEL=INFO

# https://acoustid.org/new-application
DJ_ACOUSTID_API_KEY=
